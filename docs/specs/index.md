# Specs

This section holds design specs for the project — proposals evaluated on their merits, including ones that were deferred or rejected rather than built.

- [Index Badge at Concat Time](./concat-time-index.md) — moving the `{index}` overlay badge from per-segment render to concat time so reordering rows doesn't force re-rendering unrelated segments. **Status: Deferred.**
